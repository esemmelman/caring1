# Copy to .env locally. Never commit the real value.
# Find the Session pooler connection string in Supabase > Connect.
SUPABASE_DB_URL=postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@HOST:5432/postgres

